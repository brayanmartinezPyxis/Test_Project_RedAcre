[
  {
    "name": "${service_name}",
    "image": "${account_id}.dkr.ecr.${region}.amazonaws.com/${image_name}:latest",
    "essential": true,
    "cpu": 256,
    "memory": 512,
    "portMappings": [
      {
        "containerPort": ${containerPort}
      }
    ]
  }
]
